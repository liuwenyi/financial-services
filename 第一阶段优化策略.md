# 第一阶段优化策略方案

**优化周期**: 1-2个月  
**优化重点**: 因子精细化 + 风控智能化  
**预期收益提升**: +3-5%年化  
**目标**: 年化收益达26-28%，最大回撤控制在8%以内

---

## 一、主策略因子优化（多周期因子体系）

### 1.1 信号公式体系

#### 因子1：EMA趋势因子（替代原MA10/60）
```
EMA12 = 收盘价12日指数移动平均
EMA26 = 收盘价26日指数移动平均

Trend = (EMA12 - EMA26) / EMA26 × 100
```

#### 因子2：MACD确认因子
```
MACD = EMA12 - EMA26
Signal = MACD的9日EMA

MACD_Confirm = (MACD - Signal) / 收盘价 × 100
```

#### 因子3：多周期动量因子（替代原单周期）
```
Momentum_Short = (收盘价 - 收盘价_10日前) / 收盘价_10日前 × 100
Momentum_Mid = (收盘价 - 收盘价_20日前) / 收盘价_20日前 × 100
Momentum_Long = (收盘价 - 收盘价_60日前) / 收盘价_60日前 × 100
```

#### 因子4：资金流向因子
```
典型价 = (最高价 + 最低价 + 收盘价) / 3
资金流量 = 典型价 × 成交量
MFV = 14日资金流量均值 / 30日资金流量均值
```

#### 因子5：ADX趋势强度因子
```
+DI = 上升方向指标
-DI = 下降方向指标
DX = |+DI - (-DI)| / (+DI + (-DI)) × 100
ADX = DX的14日均值
```

#### 综合信号公式
```
Combined = 0.25×Trend + 0.10×MACD_Confirm + 0.15×Momentum_Short
         + 0.15×Momentum_Mid + 0.10×Momentum_Long
         + 0.15×MFV + 0.10×ADX
```

### 1.2 参数建议

| 因子类型 | 参数名称 | 参数值 | 说明 |
|---------|---------|-------|------|
| EMA周期 | EMA_Short | 12日 | 短期趋势 |
| EMA周期 | EMA_Long | 26日 | 中长期趋势 |
| MACD参数 | Signal_Period | 9日 | 信号线周期 |
| 动量周期 | Mom_Short | 10日 | 短期动量 |
| 动量周期 | Mom_Mid | 20日 | 中期动量 |
| 动量周期 | Mom_Long | 60日 | 长期动量 |
| 资金流 | MFV_Short | 14日 | 短期资金流 |
| 资金流 | MFV_Long | 30日 | 长期资金流 |
| ADX周期 | ADX_Period | 14日 | 趋势强度指标 |
| 波动率目标 | Target_Vol | 18% | 目标年化波动率（原20%） |

### 1.3 波动率调整
```
Realized_Vol = 收益率_20日标准差 × √252
Vol_Adj = 目标波动率 / Realized_Vol

当 Realized_Vol < 10%: Vol_Adj = 1.2 (低波动，可加仓)
当 Realized_Vol < 18%: Vol_Adj = 1.0 (正常)
当 Realized_Vol < 25%: Vol_Adj = 0.7 (偏高)
当 Realized_Vol < 35%: Vol_Adj = 0.4 (高波动)
当 Realized_Vol ≥ 35%: Vol_Adj = 0.2 (极端波动)
```

### 1.4 仓位决策（连续函数）
```
# Sigmoid平滑函数
Target_Weight = 1 / (1 + e^(-15×(Combined-0.2)))

# 边界条件
当 Combined < -0.3: Target_Weight = 0.0 (空仓)
当 Combined > 0.6: Target_Weight = 0.85 (最高仓位)

# 单次最大调整
Max_Change = 15% (渐进调整，避免冲击)
```

### 1.5 预期回测指标

| 指标 | 原策略 | 第一阶段优化 | 预期提升 |
|------|--------|------------|---------|
| 年化收益率 | 23.41% | 26-28% | +2.6-4.6% |
| 最大回撤 | -9.15% | -7.5%以内 | -0.65% |
| 夏普比率 | 1.98 | 2.2-2.4 | +0.2-0.4 |
| 年交易次数 | 55次 | 40-45次 | -20% |

---

## 二、风控体系优化

### 2.1 止损机制

#### 第一层：固定止损
```
止损触发条件: 持仓 ≤ 5日
止损幅度: -8%

公式: Stop_Price = 入场价 × 0.92
```

#### 第二层：跟踪止损（5日后启用）
```
持仓5-15日: 跟踪止损 = 期间最高价 × 0.92
持仓15-30日: 跟踪止损 = 期间最高价 × 0.90
持仓>30日: 跟踪止损 = 期间最高价 × 0.88
```

#### 第三层：时间止损
```
触发条件: 持仓 ≥ 20日 且 收益率 < 2%
操作: 强制平仓
```

### 2.2 分级预警机制

```
黄色预警: 回撤 ≥ 7%
  → 建议: 减仓20%
  → 公式: 预警分 = 1

橙色预警: 回撤 ≥ 8%
  → 建议: 减仓50%
  → 公式: 预警分 = 2

红色预警: 回撤 ≥ 9%
  → 建议: 减仓80%
  → 公式: 预警分 = 3

止损线: 回撤 ≥ 10%
  → 操作: 全部清仓
  → 公式: 预警分 = 4
```

### 2.3 风控参数汇总

| 风控参数 | 设定值 | 说明 |
|---------|-------|------|
| 固定止损 | -8% | 持仓早期 |
| 跟踪止损 | -8%至-12% | 随持仓时间调整 |
| 时间止损 | 20日/收益<2% | 避免死扛 |
| 回撤预警1 | -7% | 黄色预警 |
| 回撤预警2 | -8% | 橙色预警 |
| 回撤预警3 | -9% | 红色预警 |
| 最大回撤 | -10% | 止损线 |
| 最大仓位 | 85% | 保留15%现金 |

---

## 三、再平衡优化

### 3.1 触发条件

#### 原方案
```
每周一固定再平衡
```

#### 优化方案
```
触发条件(满足任一):
1. 权重偏离 ≥ 8% 且 距上次再平衡 ≥ 3日
2. 权重偏离 ≥ 15% 且 距上次再平衡 ≥ 3日
3. 信号强度连续2日超过阈值(0.1)
4. 周一至周三且距上次再平衡 ≥ 5日
```

### 3.2 执行方式
```
分阶段执行:
阶段1: 先卖出需要清仓的标的
阶段2: 再调整需要加减仓的标的
间隔: 每个标的间隔5分钟下单
```

---

## 四、标的选择优化

### 4.1 ETF池扩展

| 代码 | 名称 | 风格 | 权重上限 |
|------|------|------|---------|
| 510300 | 沪深300ETF | 大盘价值 | 30% |
| 510310 | 沪深300ETF易 | 大盘成长 | 25% |
| 510500 | 中证500ETF | 中盘均衡 | 25% |
| 159845 | 中证1000ETF | 小盘成长 | 20% |
| 159915 | 创业板ETF | 创业板龙头 | 25% |
| 588000 | 科创50ETF | 科技创新 | 20% |
| 512010 | 医药ETF | 行业配置 | 15% |
| 159928 | 消费ETF | 消费配置 | 15% |

### 4.2 筛选规则
```
筛选步骤:
1. 计算各ETF的Momentum_Mid得分
2. 按得分降序排列
3. 选择前3名
4. 排除近5日涨幅>15%的标的(防追高)
```

---

## 五、实施清单

### 5.1 执行步骤

- [ ] 1. 实现EMA12/26趋势因子
- [ ] 2. 增加多周期动量(10/20/60日)
- [ ] 3. 引入ADX趋势强度指标
- [ ] 4. 实现Sigmoid仓位函数
- [ ] 5. 开发跟踪止损模块
- [ ] 6. 实现分级预警系统
- [ ] 7. 优化再平衡触发条件
- [ ] 8. 扩展ETF池至8只
- [ ] 9. 历史回测验证
- [ ] 10. 模拟盘验证

### 5.2 验证标准

| 指标 | 达标要求 |
|------|---------|
| 年化收益率 | ≥ 26% |
| 最大回撤 | ≤ 8% |
| 夏普比率 | ≥ 2.2 |
| 胜率 | ≥ 55% |
| 盈亏比 | ≥ 1.6 |

---

## 六、完整代码示例

```python
import pandas as pd
import numpy as np

class OptimizedStrategy:
    def __init__(self):
        # 策略参数
        self.ema_short = 12
        self.ema_long = 26
        self.macd_signal = 9
        self.mom_short = 10
        self.mom_mid = 20
        self.mom_long = 60
        self.mfv_short = 14
        self.mfv_long = 30
        self.adx_period = 14
        self.target_vol = 0.18
        
    def calculate_factors(self, df):
        """计算所有因子"""
        # EMA趋势因子
        df['EMA12'] = df['close'].ewm(span=self.ema_short).mean()
        df['EMA26'] = df['close'].ewm(span=self.ema_long).mean()
        df['Trend'] = (df['EMA12'] - df['EMA26']) / df['EMA26'] * 100
        
        # MACD确认
        df['MACD'] = df['EMA12'] - df['EMA26']
        df['Signal'] = df['MACD'].ewm(span=self.macd_signal).mean()
        df['MACD_Confirm'] = (df['MACD'] - df['Signal']) / df['close'] * 100
        
        # 多周期动量
        df['Mom_Short'] = df['close'].pct_change(self.mom_short) * 100
        df['Mom_Mid'] = df['close'].pct_change(self.mom_mid) * 100
        df['Mom_Long'] = df['close'].pct_change(self.mom_long) * 100
        
        # 资金流向
        df['Typical'] = (df['high'] + df['low'] + df['close']) / 3
        df['MoneyFlow'] = df['Typical'] * df['volume']
        df['MFV'] = df['MoneyFlow'].rolling(self.mfv_short).mean() / \
                    df['MoneyFlow'].rolling(self.mfv_long).mean()
        
        # ADX
        df = self._calculate_adx(df)
        
        return df
    
    def sigmoid_position(self, combined, threshold=0.2, steepness=15):
        """Sigmoid仓位函数"""
        pos = 1 / (1 + np.exp(-steepness * (combined - threshold)))
        
        # 边界条件
        if combined < -0.3:
            return 0.0
        elif combined > 0.6:
            return 0.85
        
        return np.clip(pos, 0, 0.85)
```

---

*第一阶段优化完成时间预期: 1-2个月*
