# 第二阶段优化策略方案

**优化周期**: 2-3个月  
**优化重点**: 策略增强 + 智能化升级  
**预期收益提升**: +2-4%年化  
**前置条件**: 第一阶段优化通过回测验证  
**目标**: 年化收益达28-30%，最大回撤控制在7%以内

---

## 一、市场状态识别系统

### 1.1 市场状态分类

基于多维度指标，将市场分为4种状态：

| 市场状态 | 判断条件 | 特征 |
|---------|---------|------|
| **强趋势** | ADX > 25 且 均线多头排列 | 单边行情，持续性强 |
| **高波动** | 波动率 > 25% 或 VIX > 25 | 震幅加大，需控仓 |
| **震荡市** | ADX < 20 且 振幅收窄 | 区间波动，反转有效 |
| **弱趋势** | 其他情况 | 方向不明 |

### 1.2 市场状态识别公式

```
# 趋势强度指数
Trend_Index = ADX(14)

# 波动率指数
Volatility_Index = Realized_Vol(20) × 100

# 均线状态
MA_Status = 
  若 MA5 > MA20 > MA60: +1 (多头)
  若 MA5 < MA20 < MA60: -1 (空头)
  否则: 0 (缠绕)

# 综合判断
def identify_market_state(trend_idx, vol_idx, ma_status, momentum):
    if trend_idx > 25 and ma_status == 1:
        return 'strong_trend'
    elif vol_idx > 25 or momentum > 0.15:
        return 'high_volatility'
    elif trend_idx < 20 and ma_status == 0:
        return 'sideways'
    else:
        return 'weak_trend'
```

### 1.3 各状态下的策略权重

| 市场状态 | 主策略 | 反转策略 | 波动策略 | 择时策略 |
|---------|-------|---------|---------|---------|
| **强趋势** | 70% | 5% | 15% | 10% |
| **高波动** | 40% | 15% | 30% | 15% |
| **震荡市** | 35% | 35% | 20% | 10% |
| **弱趋势** | 50% | 20% | 20% | 10% |

---

## 二、机器学习增强模块

### 2.1 因子重要性动态调整

使用随机森林实时评估各因子贡献度：

```python
from sklearn.ensemble import RandomForestRegressor

class AdaptiveFactorWeight:
    """因子权重自适应调整"""
    
    def __init__(self, lookback=60):
        self.lookback = lookback
        self.model = RandomForestRegressor(n_estimators=50, max_depth=5)
        self.factors = ['Trend', 'MACD_Confirm', 'Mom_Short', 
                       'Mom_Mid', 'Mom_Long', 'MFV', 'ADX']
        
    def calculate_weights(self, factor_data, returns):
        """
        基于历史数据训练模型，返回各因子权重
        """
        if len(factor_data) < self.lookback:
            return self._default_weights()
        
        X = factor_data[-self.lookback:]
        y = returns[-self.lookback:]
        
        self.model.fit(X, y)
        
        # 获取特征重要性
        importances = self.model.feature_importances_
        
        # 归一化
        weights = importances / importances.sum()
        
        return dict(zip(self.factors, weights))
    
    def _default_weights(self):
        return {
            'Trend': 0.25,
            'MACD_Confirm': 0.10,
            'Mom_Short': 0.15,
            'Mom_Mid': 0.15,
            'Mom_Long': 0.10,
            'MFV': 0.15,
            'ADX': 0.10
        }
```

### 2.2 LSTM短期预测

```python
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import LSTM, Dense

class LSTMPricePredictor:
    """LSTM价格方向预测"""
    
    def __init__(self, sequence_length=20):
        self.sequence_length = sequence_length
        self.model = self._build_model()
        
    def _build_model(self):
        model = Sequential([
            LSTM(50, return_sequences=True, input_shape=(20, 5)),
            LSTM(30),
            Dense(1, activation='sigmoid')
        ])
        model.compile(optimizer='adam', loss='binary_crossentropy')
        return model
    
    def prepare_data(self, prices, volumes, high, low):
        """准备训练数据"""
        X, y = [], []
        for i in range(self.sequence_length, len(prices)):
            features = np.column_stack([
                prices[i-self.sequence_length:i],
                volumes[i-self.sequence_length:i],
                high[i-self.sequence_length:i],
                low[i-self.sequence_length:i],
                # 添加技术指标
                self.calculate_indicators(prices[i-self.sequence_length:i])
            ])
            X.append(features)
            # 目标：次日收益为正则1，否则0
            y.append(1 if prices[i] > prices[i-1] else 0)
        
        return np.array(X), np.array(y)
    
    def predict_direction(self, recent_data):
        """
        预测次日价格方向
        返回: 0-1之间的概率
        """
        X = self.prepare_single(recent_data)
        prob = self.model.predict(X)
        return prob[0][0]
```

### 2.3 机器学习参数

| 模块 | 参数 | 设定值 |
|------|------|-------|
| 随机森林 | n_estimators | 50 |
| 随机森林 | max_depth | 5 |
| 随机森林 | lookback | 60日 |
| LSTM | sequence_length | 20 |
| LSTM | units_1 | 50 |
| LSTM | units_2 | 30 |
| LSTM | epochs | 50 |

---

## 三、资金流向因子增强

### 3.1 北向资金因子

```python
def calculate_north_flow_factor(stock_code, window=5):
    """
    计算北向资金净流入因子
    
    公式:
    North_Flow = (北向资金5日净买入) / (5日平均成交额)
    """
    north_buy = get_north_money_flow(stock_code, window='5d')
    avg_volume = get_average_volume(stock_code, window=5)
    
    north_flow = north_buy / avg_volume
    
    return north_flow  # >0为净流入
```

### 3.2 ETF资金流向因子

```python
def calculate_etf_money_flow(etf_code, window=5):
    """
    计算ETF资金净流入因子
    
    公式:
    ETF_Flow = (ETF份额变化 × 单位净值) / 总资产
    """
    share_change = get_etf_share_change(etf_code, window)
    nav = get_etf_nav(etf_code)
    total_assets = get_etf_total_assets(etf_code)
    
    etf_flow = (share_change * nav) / total_assets
    
    return etf_flow
```

### 3.3 综合资金因子

```
Money_Factor = 0.4 × North_Flow + 0.4 × ETF_Flow + 0.2 × Volume_MFV
```

---

## 四、策略权重动态调整

### 4.1 动态权重公式

```python
def get_dynamic_strategy_weights(market_state, trend_strength, volatility_level):
    """
    根据市场状态动态调整策略权重
    
    参数:
    - market_state: 市场状态识别结果
    - trend_strength: 趋势强度(ADX)
    - volatility_level: 波动率水平
    """
    
    # 基础权重
    base_weights = {
        'main': 0.60,
        'reversal': 0.15,
        'volatility': 0.15,
        'timing': 0.10
    }
    
    # 根据市场状态调整
    if market_state == 'strong_trend':
        return {
            'main': 0.70,      # 趋势强，主策略为主
            'reversal': 0.05,  # 反转策略降低
            'volatility': 0.15,
            'timing': 0.10
        }
    
    elif market_state == 'high_volatility':
        return {
            'main': 0.40,      # 高波动，主策略降低
            'reversal': 0.15,
            'volatility': 0.30,  # 波动策略提高
            'timing': 0.15
        }
    
    elif market_state == 'sideways':
        return {
            'main': 0.35,      # 震荡市，反转为主
            'reversal': 0.35,
            'volatility': 0.20,
            'timing': 0.10
        }
    
    else:  # weak_trend
        return {
            'main': 0.55,
            'reversal': 0.18,
            'volatility': 0.17,
            'timing': 0.10
        }


def adjust_by_trend_strength(base_weights, trend_strength):
    """
    根据趋势强度微调权重
    """
    if trend_strength > 30:
        # 强趋势，增强主策略
        base_weights['main'] = min(base_weights['main'] + 0.05, 0.80)
    elif trend_strength < 15:
        # 弱趋势，降低主策略
        base_weights['main'] = max(base_weights['main'] - 0.10, 0.30)
    
    return base_weights
```

### 4.2 权重调整决策树

```
市场状态识别
    │
    ├─→ 强趋势(ADX>25)
    │       └─→ 主策略权重 +5%, 反转策略 -10%
    │
    ├─→ 高波动(Vol>25%)
    │       └─→ 波动策略权重 +15%, 主策略 -20%
    │
    ├─→ 震荡市(ADX<20)
    │       └─→ 反转策略权重 +20%, 主策略 -25%
    │
    └─→ 弱趋势(其他)
            └─→ 均衡配置
```

---

## 五、辅助策略参数优化

### 5.1 RSI自适应参数

```python
def adaptive_rsi(df, market_state):
    """
    根据市场状态自适应调整RSI参数
    """
    if market_state == 'high_volatility':
        period = 21
        ob = 75
        os = 25
    elif market_state == 'strong_trend':
        period = 14
        ob = 80
        os = 20
    elif market_state == 'sideways':
        period = 10
        ob = 70
        os = 30
    else:
        period = 14
        ob = 70
        os = 30
    
    return calculate_rsi(df['close'], period), ob, os
```

### 5.2 KDJ平滑优化

```python
def smoothed_kdj(high, low, close, n=9, m1=3, m2=3):
    """
    平滑处理的KDJ指标
    """
    # 计算RSV
    lowest_low = low.rolling(n).min()
    highest_high = high.rolling(n).max()
    rsv = (close - lowest_low) / (highest_high - lowest_low) * 100
    
    # EMA平滑K
    k = rsv.ewm(alpha=1/m1, adjust=False).mean()
    
    # EMA平滑D
    d = k.ewm(alpha=1/m2, adjust=False).mean()
    
    # J值
    j = 3 * k - 2 * d
    
    return k, d, j
```

### 5.3 优化后参数表

| 辅助策略 | 参数 | 强趋势 | 高波动 | 震荡 | 弱趋势 |
|---------|------|-------|-------|------|-------|
| **RSI周期** | period | 14 | 21 | 10 | 14 |
| **RSI超买** | overbought | 80 | 75 | 70 | 70 |
| **RSI超卖** | oversold | 20 | 25 | 30 | 30 |
| **KDJ参数** | K_period | 9 | 12 | 9 | 9 |
| **波动率阈值** | vol_low | 15% | 20% | 12% | 15% |
| **波动率阈值** | vol_high | 25% | 30% | 20% | 25% |

---

## 六、完整信号公式

### 6.1 综合信号计算

```python
def calculate_enhanced_signal(df, market_state, factor_weights, ml_direction):
    """
    增强版综合信号
    
    参数:
    - df: 行情数据
    - market_state: 市场状态
    - factor_weights: 因子权重(来自机器学习)
    - ml_direction: LSTM预测方向(0-1)
    """
    
    # 1. 基础因子得分
    factors = calculate_factors(df)
    
    # 2. 加权因子得分
    weighted_score = sum([
        factor_weights['Trend'] * factors['Trend'],
        factor_weights['MACD_Confirm'] * factors['MACD_Confirm'],
        factor_weights['Mom_Short'] * factors['Mom_Short'],
        factor_weights['Mom_Mid'] * factors['Mom_Mid'],
        factor_weights['Mom_Long'] * factors['Mom_Long'],
        factor_weights['MFV'] * factors['MFV'],
        factor_weights['ADX'] * factors['ADX']
    ])
    
    # 3. ML预测信号融合
    ml_signal = (ml_direction - 0.5) * 0.2  # -0.1到+0.1
    
    # 4. 市场状态调整
    state_adjustment = get_state_adjustment(market_state)
    
    # 5. 最终信号
    final_signal = weighted_score + ml_signal + state_adjustment
    
    return final_signal


def get_state_adjustment(market_state):
    """
    市场状态调整
    """
    adjustments = {
        'strong_trend': 0.05,   # 顺势增强
        'high_volatility': -0.05, # 高波动降低
        'sideways': 0.0,
        'weak_trend': 0.0
    }
    return adjustments.get(market_state, 0)
```

### 6.2 仓位计算

```python
def calculate_position_v2(final_signal, market_state, volatility):
    """
    增强版仓位计算
    """
    # 基础仓位
    base_position = sigmoid_position(final_signal, threshold=0.2, steepness=15)
    
    # 市场状态调整
    state_multiplier = {
        'strong_trend': 1.1,     # 强趋势可略超限
        'high_volatility': 0.7,   # 高波动降仓
        'sideways': 0.9,
        'weak_trend': 1.0
    }
    
    # 波动率调整
    if volatility < 0.15:
        vol_multiplier = 1.0
    elif volatility < 0.25:
        vol_multiplier = 0.8
    else:
        vol_multiplier = 0.6
    
    # 综合仓位
    target_position = base_position * state_multiplier[market_state] * vol_multiplier
    
    # 硬限制
    max_position = 0.85
    return min(target_position, max_position)
```

---

## 七、预期回测指标

### 7.1 指标对比

| 指标 | 第一阶段优化 | 第二阶段优化 | 提升 |
|------|------------|------------|------|
| **年化收益率** | 26-28% | 28-32% | +2-4% |
| **最大回撤** | -7.5% | -7%以内 | -0.5% |
| **夏普比率** | 2.2-2.4 | 2.4-2.8 | +0.2-0.4 |
| **卡玛比率** | 3.0+ | 3.5+ | +0.5 |
| **胜率** | 55% | 58% | +3% |

### 7.2 预期年化收益分解

| 收益来源 | 贡献度 |
|---------|-------|
| 第一阶段基础收益 | 26-28% |
| 市场状态识别增强 | +1-2% |
| 机器学习因子优化 | +1-1.5% |
| 动态权重调整 | +0.5-1% |
| **合计** | **28-32%** |

---

## 八、实施清单

### 8.1 执行步骤

- [ ] 1. 开发市场状态识别模块
- [ ] 2. 实现动态策略权重系统
- [ ] 3. 部署随机森林因子权重模型
- [ ] 4. 开发LSTM价格预测模块
- [ ] 5. 接入北向资金数据源
- [ ] 6. 实现自适应RSI参数
- [ ] 7. 优化KDJ平滑计算
- [ ] 8. 综合信号融合测试
- [ ] 9. 历史回测验证(2018-2026)
- [ ] 10. 样本外验证(2026最新数据)
- [ ] 11. 模拟盘实盘验证

### 8.2 数据需求

| 数据类型 | 来源 | 用途 |
|---------|------|------|
| 日线行情 | 聚宽/AkShare | 基础因子计算 |
| 北向资金 | 东方财富 | 资金流向因子 |
| ETF份额 | 上交所/深交所 | 资金净流入 |
| 波动率指数 |Wind | 市场恐慌指数 |

### 8.3 验证标准

| 指标 | 达标要求 |
|------|---------|
| 年化收益率 | ≥ 28% |
| 最大回撤 | ≤ 7% |
| 夏普比率 | ≥ 2.4 |
| 样本外验证 | 收益不衰减>30% |

---

## 九、风险提示

### 9.1 机器学习风险

```
⚠️ 过拟合风险:
- 限制模型复杂度(max_depth≤5)
- 使用滚动窗口训练
- 样本外验证必须通过

⚠️ 预测失效风险:
- 当市场结构发生重大变化时，LSTM可能失效
- 需设置人工干预机制
- 持续监控预测准确率
```

### 9.2 数据风险

```
⚠️ 数据延迟:
- 实时数据可能有秒级延迟
- 建议使用Level-2或直接行情接口

⚠️ 数据缺失:
- 北向资金节假日无数据
- 需设置缺失值处理逻辑
```

---

*第二阶段优化完成时间预期: 2-3个月*  
*前提条件: 第一阶段回测验证通过*
